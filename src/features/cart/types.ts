export type CartItemTypes = {
    addIngredients?: string[];
    name: string;
    pizzaId?: number;
    quantity: number;
    removeIngredients?: string[];
    totalPrice: number;
    unitPrice: number;
};
